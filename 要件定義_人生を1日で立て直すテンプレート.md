# 要件定義：人生を1日で立て直すテンプレート（毎日通知 + Googleドキュメント管理）

## 1. 目的

「人生を1日で立て直すテンプレート」を毎日実行できる仕組みを作る。

- 決まった時刻に問いがプッシュ通知で届く
- 回答はその日専用のGoogleドキュメントに記入する
- 記録はGoogle Drive上に日付フォルダで整理され、あとから振り返れる

## 2. 全体像

```
毎朝9時（JST）
  └─ その日のフォルダ + テンプレ入りGoogleドキュメントを自動作成
  └─ 朝の問いをプッシュ通知（ドキュメントへのリンク付き）

11時 / 12時 / 15時 / 17時
  └─ 昼の問いをプッシュ通知（当日ドキュメントへのリンク付き）

21時
  └─ 夜の振り返りの問いをプッシュ通知
  └─ 朝に書いたバッドエンド／ハッピーエンドを読み取り、通知に引用して振り返りを助ける
```

## 3. Google Drive フォルダ構成

```
人生を1日で立て直す/          ← 親フォルダ（初回のみ自動作成）
├─ 2026-08-15/               ← 毎朝9時に自動作成される日付フォルダ
│   └─ 2026-08-15 立て直しシート   ← テンプレ全文入りGoogleドキュメント
├─ 2026-08-16/
│   └─ 2026-08-16 立て直しシート
└─ ...
```

- 日付フォルダ名：`YYYY-MM-DD`
- ドキュメント名：`YYYY-MM-DD 立て直しシート`
- ドキュメントにはテンプレート全文（朝・昼・夜・縛りの全項目）が最初から入っており、ユーザーは「→」の後に記入していくだけ

## 4. 通知スケジュール（Routine設計）

すべて毎日実行。cronはUTC指定（JST = UTC+9）。

| # | JST | UTC cron | 役割 | 通知内容 |
|---|-----|----------|------|---------|
| 1 | 9:00 | `0 0 * * *` | 【朝】未来を頭の中に作る | フォルダ・ドキュメント作成後、①5年後のバッドエンド ②3年後のハッピーエンド の問い + ドキュメントリンク |
| 2 | 11:00 | `0 2 * * *` | 【昼】① | 「今やっていることで『避けていること』は何？」 |
| 3 | 12:00 | `0 3 * * *` | 【昼】② | 「今日の午前中の行動を他人が見たら、『この人は何を求めている』と思われる？」 |
| 4 | 15:00 | `0 6 * * *` | 【昼】③ | 「今、自分はハッピーエンドに向かっている？それともバッドエンドに向かっている？」 |
| 5 | 17:00 | `0 8 * * *` | 【昼】④ | 「一見重要でないように見えて、実は一番重要なことは何？」 |
| 6 | 21:00 | `0 12 * * *` | 【夜】振り返り | 夜の問い①〜⑥ + 縛り①〜③ + 朝の記入内容の引用 + ドキュメントリンク |

- 通知チャネル：プッシュ通知（スマホ）。メールは使わない
- 朝(9時)と夜(21時)：Google Drive操作が必要なため、Drive接続済みのセッションに発火し、PushNotificationツールで通知を送る
  （※この組織設定では新規セッション型RoutineにDriveコネクタを付与できないため）
- 昼の4本：通知のみのため、新規セッション実行＋完了プッシュ通知（旧内省リマインダーと同方式）。通知文に「Driveの今日の立て直しシートに記入」の誘導を含める

## 5. 各時刻の処理詳細

### 9時（朝）
1. **前日のシートを読み、【夜】⑤の目標3つ（1年後／1か月後／明日）を抜き出す**
2. 当日フォルダ `YYYY-MM-DD` を作成（既にあれば再利用）
3. テンプレ全文入りGoogleドキュメント `YYYY-MM-DD 立て直しシート` を作成
4. プッシュ通知：**昨日立てた「今日の目標」** + 朝の問い2つ
5. セッションメッセージ：昨日の目標3つの全文 + 朝の問い + 当日ドキュメントリンク

### 11時〜17時（昼）
1. 当日ドキュメントを検索
2. プッシュ通知：その時刻の問い + ドキュメントリンク
3. ドキュメントが見つからない場合（朝の実行失敗など）は問いのみ通知

### 21時（夜）
1. 当日ドキュメントを検索し、内容を読み取る
2. 朝に記入されたバッドエンド／ハッピーエンドがあれば、通知に短く引用（③④の「一行要約」を助けるため）
3. プッシュ通知：夜の問い①〜⑥ + 縛り①〜③ + ドキュメントリンク

## 6. 既存Routineの扱い

現在、旧バージョンの「内省リマインダー」5本（11時・13時・15時・17時・21時）が稼働中。

- 本システムの6本に**置き換える**（旧5本は削除）
- 変更点：
  - 9時（朝）を新設
  - 13時 → 12時に変更
  - 全時刻の問い文言をテンプレート最新版に合わせて更新
  - 通知だけでなくGoogleドキュメント連携を追加
- ※既存Routineはこのセッションからの更新が権限エラーになるため「削除 → 再作成」で対応する（削除操作はユーザー承認後に実施）

## 7. ユーザーの運用フロー

1. 朝9時の通知を受け取り、リンクからドキュメントを開いて朝の2問に記入
2. 昼の通知が来るたびに、同じドキュメントの該当欄に記入
3. 夜21時の通知で1日を振り返り、目標と縛りを記入
4. 翌朝はまた新しいドキュメントが作られる（前日のものはフォルダに残る）

## 8. 制約・前提

- cronの粒度は1分単位、時刻は多少（数分）ずれることがある
- 通知はClaudeアプリのプッシュ通知として届く（アプリの通知許可が必要）
- Google Driveコネクタの接続が有効であることが前提。切れた場合、朝のドキュメント作成が失敗し、その日は通知のみになる
- ドキュメントへの記入はユーザーが手動で行う（自動記入はしない）

## 9. 実装結果（2026-08-15 実施済み）

1. 旧「内省リマインダー」5本 → 実装時点で既に削除済みであることを確認
2. 新Routine 6本を作成
   - 朝9時・夜21時：Drive接続セッションに発火（PushNotificationで通知）
   - 11/12/15/17時：新規セッション＋完了プッシュ通知
3. 親フォルダ「人生を1日で立て直す」を作成
   https://drive.google.com/drive/folders/1D_coG1N6dtAaRGFV23FxNZemSr3npZee
4. 本日分（2026-08-15）のフォルダとテンプレ入りドキュメントを作成済み
   https://docs.google.com/document/d/1UFRikaDBCPzcecOvHfbPAl9DBauMBru-NdaWPgkBIvE/edit

## 10. 通知到達テスト（2026-08-15 実施）

### 10.1 通知設定の確認結果

Routine 7本（立て直し6本 + gmailチェッカー）すべて `push: true` / `email: false`。
新たにONにする必要のあるものは無かった。

### 10.2 手動発火テスト

立て直し6本を手動発火（gmailチェッカーはメールのアーカイブなど実操作が走るため除外）。
発火リクエストは6本とも受理され、`last_fired_at` が更新されたことを確認。

| Routine | 実行方式 | 通知の出し方 |
|---|---|---|
| 11 / 12 / 15 / 17 時 | 新規セッション（毎回作り直し） | Routineの完了プッシュ通知 |
| 朝9時 / 夜21時 | 常駐セッション `session_015cqGkDZKjvUtXMrRavKE3J` に発火 | セッション内の PushNotification ツール |

### 10.3 判明した問題：朝9時・夜21時の実行基盤が不安定

朝9時・夜21時の2本は、Google Drive操作のために常駐セッションへ発火する設計になっている
（新規セッション型RoutineにDriveコネクタを付与できないため。5節参照）。
このテスト時点で、その常駐セッションは以下の状態だった。

- `status_bucket`: FAILED（`stop_reason=tool_use` で停止）
- `connection_status`: disconnected
- `permission_mode`: **default**
- 発火後も `updated_at` が進まない（＝発火を受けて処理が走った形跡がない）

`permission_mode` が default のままだと、無人実行中にDriveツールの権限確認で停止し、
承認する人がいないため止まったままになる。これが `stop_reason=tool_use` の停止と整合する。

つまり **昼の4本は独立して動くが、朝・夜の2本は常駐セッションの状態に依存しており、
そこが落ちていると通知が出ない**。

### 10.4 対応候補（未実施）

1. 常駐セッションの `permission_mode` を auto にして、Driveツールで停止しないようにする
2. 朝・夜もDrive連携を諦めて新規セッション型に寄せ、通知だけ確実に飛ばす
   （シート作成は別手段にするか、手動運用に戻す）
3. 常駐セッション方式を維持しつつ、セッションが落ちていた場合に備えて
   通知のみのフォールバックRoutineを別に持つ

## 11. 朝・夜Routineの作り直し（2026-08-15 実施）

### 11.1 検証：新規セッション型にDriveコネクタは付けられない（5節の記述が正しかった）

一度「5節の“付与できない”は誤りではないか」と疑ったが、実際に試したところ組織設定で
ブロックされていることを確認した。

```
create_trigger: the connectors parameter is not available for this organization.
Omit the connectors parameter.
```

`gmailチェッカー` にGmailコネクタが付いているのは、あれが Web UI 経由で作られたRoutine
（`created_via: "http_api"`）だから。立て直し系6本は `meta_mcp`（セッション経由）で作られており、
作成経路によって使える機能が違う。

→ **セッションからはDriveコネクタ付きRoutineを作れない。Web UIからなら可能と思われる。**

### 11.2 実施内容：通知の復旧を優先

「明日の朝9時に何も届かない」状態を止めることを優先し、Drive連携を一旦切り離した。

| 対象 | 操作 |
|---|---|
| 立て直し 朝9時（朝の問い） | **新規作成** `trig_0167gXKZSfjmpj3pTd6JYsDM` / `0 0 * * *` / 新規セッション型・push ON |
| 立て直し 夜21時（振り返り） | **新規作成** `trig_01XC81GbXWP8HtXPk9K5sc9A` / `0 12 * * *` / 新規セッション型・push ON |
| 旧 朝9時 `trig_014GXdAPLiNGRQbcGGUJVu6K` | **無効化**（削除はせず `enabled: false`、名前に「[停止中・旧]」を付与） |
| 旧 夜21時 `trig_01GKCsReht52oYtN3Ngo2LuH` | **無効化**（同上） |

新2本は、実際に動作している昼4本と全く同じ構成（新規セッション型＋完了プッシュ通知、
プロンプトは問い文のみ）に揃えた。常駐セッションへの依存が無くなった。

### 11.3 この変更で失われた機能

- 朝9時の当日フォルダ・シートの自動作成 → **手動作成に戻る**
- 前日の【夜】⑤の目標を朝の通知で読み上げる機能 → 無くなる
- 夜21時に朝の記入内容を引用する機能 → 無くなる

通知文には「今日の立て直しシートに記入」の誘導を残している。

### 11.4 残課題：チャットの返信をDriveへ自動反映したい（未実現）

ユーザー要望：「このチャットに答えることでドライブにも自動で反映されることがベスト」。

実現には、通知を受け取ったセッション自体がDriveに繋がっている必要がある。
11.1 の制約により、セッション経由では作れない。

対応方針：**claude.ai のRoutine設定画面（Web UI）から、朝・夜のRoutineに
Google Driveコネクタを付ける**。付けられたら、プロンプトに以下を追加する。

```
【返信の自動記入】ユーザーがこのチャットで問いに答えたら、その回答を今日の
「YYYY-MM-DD 立て直しシート」の該当項目の「→」の直後に追記する。
- 言い回しを要約・整形せず原文のまま記入する
- 既に記入がある場合は上書きせず、先に確認する
- どの項目に何を記入したかを1〜2行で報告する
```

なお8節の「ドキュメントへの記入はユーザーが手動で行う（自動記入はしない）」は、
この要望により方針変更となる。実現時に8節を更新すること。

### 11.5 記録の誤り訂正

9節に記載のドキュメントURL `1UFRikaDBCPzcecOvHfbPAl9DBauMBru-NdaWPgkBIvE` は存在しない。
2026-08-15 のシートの正しいIDは `1e_Y5eX23hFvXoL0-Ebp6dRX1_156hdCpmWbnKPWOsAU`。
https://docs.google.com/document/d/1e_Y5eX23hFvXoL0-Ebp6dRX1_156hdCpmWbnKPWOsAU/edit

### 11.6 テンプレート全文（プロンプト埋め込み用・実シートより取得）

新規セッションには過去の会話履歴が無いため、シート作成を自動化する場合は
以下の全文をプロンプトに埋め込む必要がある（旧プロンプトは「この会話の過去の
シート作成時と同じ本文を使う」と書かれており、新規セッションでは機能しない）。

```
【人生を1日で立て直すテンプレート】YYYY-MM-DD

ーーー
【朝】未来を頭の中に作る（9時）
ーーー
① 何も変えずに過ごしたら訪れるであろう、5年後のバッドエンドはどんな世界？
→
② 3年後に送りたいハッピーエンドはどんな世界？
→

ーーー
【昼】無意識の行動を遮断する
ーーー
① 今やっていることで「避けていること」は何？（11時）
→
② 今日の午前中の行動を他人が見たら、「この人は何を求めている」と思われる？（12時）
→
③ 今、自分はハッピーエンドに向かっている？それともバッドエンドに向かっている？（15時）
→
④ 一見重要でないように見えて、実は一番重要なことは何？（17時）
→

ーーー
【夜】振り返り／明日への具体的なアクションに落とし込む（21時）
ーーー
① 今日いちばん「生きている」と感じた瞬間は？
→
② 今日いちばん「死んでいる」と感じた瞬間は？
→
③ 朝に書いたバッドエンドを一行で要約すると？
→
④ 朝に書いたハッピーエンドを一行で要約すると？
→
⑤ ハッピーエンドに向かうための具体的な目標を設定する
（状況に応じて軌道修正しながら進めてよい）
・1年後の目標
→
・1か月後の目標
→
・明日の目標
→
⑥ 真の敵（ハッピーエンドに向かう私を邪魔する"私の性質"）に名前をつけるとしたら？
→

ーーー
【縛り（明日から意識すること）】
ーーー
① ハッピーエンド側に寄せるために意識することは？
→
② バッドエンド側に傾きそうになったとき、どう切り替える？
→
③ それでもうまくいかなかった場合、どう再スタートする？
→
```
